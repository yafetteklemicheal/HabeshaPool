<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PlayerSelectionActivity">

    <!-- Title: Welcome -->
    <TextView
        android:id="@+id/title_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="48dp"
        android:text="Welcome to Habesha Pool"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

    <!-- Language prompt -->
    <TextView
        android:id="@+id/text_language_prompt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Please select your language"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@id/title_text"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

    <!-- Language buttons: Eng Amh (spread left-right) -->
    <Button
        android:id="@+id/button_english"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="8dp"
        android:text="Eng"
        app:layout_constraintTop_toBottomOf="@id/text_language_prompt"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/button_amharic"
        app:layout_constraintHorizontal_chainStyle="spread" />

    <Button
        android:id="@+id/button_amharic"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="32dp"
        android:text="Amh"
        app:layout_constraintTop_toBottomOf="@id/text_language_prompt"
        app:layout_constraintLeft_toRightOf="@id/button_english"
        app:layout_constraintRight_toRightOf="parent" />

    <!-- Players prompt -->
    <TextView
        android:id="@+id/text_players_prompt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="Please select number of players"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@id/button_english"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

    <!-- GridLayout for player buttons (populated dynamically using images) -->
    <GridLayout
        android:id="@+id/player_buttons_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:columnCount="3"
        android:padding="6dp"
        app:layout_constraintTop_toBottomOf="@id/text_players_prompt"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="12dp" />

</androidx.constraintlayout.widget.ConstraintLayout>